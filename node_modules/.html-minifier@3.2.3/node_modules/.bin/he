#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.he@1.1.0/bin/he" "$@"
  ret=$?
else 
  node  "$basedir/../../../.he@1.1.0/bin/he" "$@"
  ret=$?
fi
exit $ret
