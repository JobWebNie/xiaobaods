#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.mkdirp@0.5.1/bin/cmd.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../.mkdirp@0.5.1/bin/cmd.js" "$@"
  ret=$?
fi
exit $ret
