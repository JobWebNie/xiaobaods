#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.uglify-js@2.7.5/bin/uglifyjs" "$@"
  ret=$?
else 
  node  "$basedir/../../../.uglify-js@2.7.5/bin/uglifyjs" "$@"
  ret=$?
fi
exit $ret
